<div class="container py-4">
  <h1 class="h3 mb-3">Dashboard</h1>
  {{#if user}}
    <p class="mb-1"><strong>Usuario:</strong> {{user.email}}</p>
    <p class="mb-3"><strong>Rol:</strong> {{user.role}}</p>
  {{else}}
    <p>No hay sesion activa.</p>
  {{/if}}
</div>
